import { DefaultLayout, HeaderOnlyLayout, AuthLayout } from '../../layouts';

const layouts = {
    defaultLayout: DefaultLayout,
    headerOnlyLayout: HeaderOnlyLayout,
    authLayout: AuthLayout
};

export default layouts;
